import object_classes
from vpython import *
from datetime import datetime


# for i in range(1, 102):
#     scene = canvas()
#     execTime = datetime.now()
#     # for j in range(i):
#     # cylinder(size=vector(2,2,2), color= color.blue, visible=True)
#     # sizeVector = vector(1.5,10,14.5)
#     # cylinder(size=sizeVector,
#     #                color=color.magenta, visible=True)
#     # size = 1
#     # sizeVector = vector(size * 11, size * 11, 5)
#     # obstacle = box(size=sizeVector,
#     #                     color=color.cyan, visible=True)
#     # corner = CornerPipeTemp = extrusion(path=paths.arc(radius=2.6 * 3, angle1=-0.09, angle2=pi / 2 + 0.092),
#     #                        shape=shapes.arc(radius=2.6, angle1=0, angle2=2 * pi, rotate=pi),
#     #                        color=color.cyan, visible = True)
#     execTime = datetime.now()-execTime
#     note = open("refreshTestTime.txt", "a")
#     note.write(str(execTime.total_seconds())+"\n")


